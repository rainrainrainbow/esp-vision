# ESP-VISION Stubs

These `.pyi` files describe ESP-VISION MicroPython modules for IDE completion and static analysis. Point your tool to this directory, for example by setting Pyright or Pylance `stubPath` to `stubs`.
