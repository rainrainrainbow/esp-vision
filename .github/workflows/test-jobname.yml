name: Test Job Name

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build Something
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "done"
